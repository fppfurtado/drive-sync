build/
*.egg-info/
__pycache__/
*.pyc
.pytest_cache/

# Local config (copy config/config.yaml.example to ~/.config/drive-sync/config.yaml)
config/config.yaml

.claude/

.worktrees/

# throughline session-notes (ephemeral; durable output lands in docs/ + tracker)
.throughline/
